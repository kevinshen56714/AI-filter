import { Filters } from '@/types/filters';

export const AIFilter = ({ onFilterChange }: { onFilterChange: (filters: Filters) => void }) => {
  return <div className="w-3/5 mb-12">Implement me!</div>;
};
